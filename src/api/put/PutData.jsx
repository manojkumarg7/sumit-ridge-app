import React, { useEffect, useState } from "react";
import { useParams, useNavigate } from "react-router-dom";

const PutData = () => {
  // const PutData = ({formData && dogId}) => {
  useEffect(() => {
    const updateData = async () => {
      // if (formData && dogId) {
      // }
    };
  });
};

export default PutData;
